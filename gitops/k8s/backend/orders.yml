apiVersion: apps/v1
kind: Deployment

metadata:
  name: orders
  namespace: boutique

spec:
  replicas: 1

  selector:
    matchLabels:
      app: orders

  template:
    metadata:
      labels:
        app: orders

    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox
          command:
            - sh
            - -c
            - >
              until nc -z boutique-postgres 5432;
              do
                echo waiting for postgres;
                sleep 5;
              done;

      containers:
        - name: orders
<<<<<<< HEAD
          image: 995938832906.dkr.ecr.ap-south-1.amazonaws.com/orders:15c2fe53e6f0006b225b44ff3e23a58ed2c44a41
=======
          image: 995938832906.dkr.ecr.ap-south-1.amazonaws.com/orders:latest
>>>>>>> 8b3ebc3 (initial ai devsecops setup)

          ports:
            - containerPort: 3005

          env:
            - name: NODE_ENV
              value: development

            - name: PORT
              value: "3005"

            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: boutique-secrets
                  key: ORDERS_DB_URL

            - name: PRODUCTS_SERVICE_URL
              value: "http://product-service:3003"

          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 30
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 10
            periodSeconds: 5

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"

---
apiVersion: v1
kind: Service

metadata:
  name: orders
  namespace: boutique

spec:
  selector:
    app: orders

  ports:
    - protocol: TCP
      port: 3005
      targetPort: 3005

  type: ClusterIP